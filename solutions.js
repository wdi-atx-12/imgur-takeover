console.log('Defining functions!');

function changeNavColor() {
  // change the navbar to a different color
};

document.getElementById("topbar").style.background = 'red';

document.querySelector("body").innerHTML = "<h1>This Page is now under Devin's control!<h1>"+document.querySelector("body").innerHTML

var imgs = document.querySelectorAll('cards .post');
for (var i = 0; i < images.length / 2; i++) {
  images[i].parentNode.removeChild(images[i]);
    images[i].pop;
    console.log(images);
}
